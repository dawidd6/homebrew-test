name: Test

on:
  push:
    branches:
      - master

jobs:
  cancel:
    runs-on: ubuntu-16.04
    if: false
    steps:
      - run: gcc --version
      - run: which -a gcc
  notcancel:
    needs: cancel
    runs-on: ubuntu-16.04
    steps:
      - run: echo run
