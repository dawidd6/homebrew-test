name: Test

on:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ls -A
      - uses: actions/checkout@v2
        with:
          repository: dawidd6/lazygit-debian
      - run: ls -A
